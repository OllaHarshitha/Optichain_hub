<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display Product Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5; /* Light gray background */
            padding: 20px;
            margin: 0;
        }

        h1 {
            text-align: center;
            color: #333; /* Dark gray color */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd; /* Light gray border */
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2; /* Lighter gray background for table header */
            color: #333; /* Dark gray color for table header text */
        }

        tr:nth-child(even) {
            background-color: #f9f9f9; /* Alternate row background color */
        }

        a {
            text-decoration: none;
            color: #007bff; /* Blue color for links */
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <h1>Display Product Information</h1>
    <table>
        <tr>
            <th>Product ID</th>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Quantity</th>
        </tr>
        <% data.forEach(products => { %>
        <tr>
            <td><%= products.product_id %></td>
            <td><%= products.name %></td>
            <td><%= products.description %></td>
            <td><%= products.price %></td>
            <td><%= products.quantity %></td>
        </tr>
        <% }) %>
    </table>
    <p><a href="http://localhost:8080/">| Go home |</a></p>
</body>

</html>
